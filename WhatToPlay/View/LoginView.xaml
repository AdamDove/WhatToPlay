<Window x:Class="WhatToPlay.View.LoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WhatToPlay.View"
        xmlns:ViewModel="clr-namespace:WhatToPlay.ViewModel"
        mc:Ignorable="d"
        Title="Login" Height="300" Width="300">

    <Window.DataContext>
        <ViewModel:LoginViewModel/>
    </Window.DataContext>

    <!-- Login -->
    <UniformGrid Rows="3" Columns="2" VerticalAlignment="Center" HorizontalAlignment="Center" Visibility="{Binding LoginRequired, Converter={StaticResource BoolToVisibility}}">
        <TextBlock Text="Steam UserName"/>
        <TextBox Name="txtUserName" Text="{Binding UserName}"/>
        <TextBlock Text="Steam Password"/>
        <PasswordBox Name="PasswordBox" />
        <CheckBox Name="cbxRememberMe" Content="Remember Me" IsChecked="{ Binding RememberMe }" />
        <Button Content="Login" Click="Button_Click"/>

    </UniformGrid>

</Window>
